# dashboards.yaml в ./grafana/provisioning/dashboards/
apiVersion: 1

providers:
  - name: 'Default Dashboard' 
    orgId: 1
    folder: '' 
    type: file
    disableDelete: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards 
      intervalSeconds: 10 